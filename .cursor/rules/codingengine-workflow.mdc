---
description: CodingEngine 核心工作流 — 编排与原子提交
alwaysApply: true
---

# CodingEngine 工作流规则

## 复杂任务必须走 codingengine

- 涉及 **3+ 文件** 或 **跨模块** 的改动，必须走 codingengine 流程
- 规划阶段必须产出 `plan.json`，含：任务依赖图、context_limit 预估、atomic_commit 标记

## 开发阶段

- 按 plan.json 执行，每个任务独立 commit
- 每个 task 完成后：`git add -A && git commit -m "[codingengine] <task_id>"`

## 验证失败必须迭代

- verify 失败时：诊断 → 回滚失败任务 → 重试（最多 3 轮）
- 不得跳过验证或忽略失败
