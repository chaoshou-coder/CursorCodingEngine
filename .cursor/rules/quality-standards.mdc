---
description: CodingEngine 质量标准 — lint、测试、安全底线
alwaysApply: true
---

# 质量标准规则

## 编辑后立即验证

- 每次文件修改后立即跑 lint（`ruff check`），不攒到最后
- 若引入 linter error，立即修复

## 测试覆盖

- 新增公开函数/类时，先写测试再写实现（TDD）
- 至少 1 个正常路径 + 1 个边界条件

## 安全底线

- 不硬编码 API Key、密码等敏感信息
- 用户输入需校验，避免注入
